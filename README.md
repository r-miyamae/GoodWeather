# GoodWeather
##使っているもの

- spring-boot
- Gradle
- jetty 
- lombok（入れてるだけ）
- gson
- jsonic

SpringBoot起動すれば多分動く

```
cd {GoodWeatherRootDir}/
./gradlew bootRun
```

ブラウザを使って `localhost:8080/test` にアクセスすることで動作確認可能
